# Credentials for mcp-config, which refuses an anonymous caller: it hands out this
# service's database password and JWT signing key. Defined in mcp-config/.env; this is a
# copy so the service can start on its own.
CONFIG_USER=mcp
CONFIG_PASSWORD=
